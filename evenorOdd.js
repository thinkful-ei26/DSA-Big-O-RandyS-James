function isEven(value){
    if (value % 2 == 0){
      return true;
    }
    else
      return false;
  }
/* 
takes an input number performs 1 operation so O(1) constant time because no matter how big the number is only 1 operation is made.
best case is the same as the worst case
*/
